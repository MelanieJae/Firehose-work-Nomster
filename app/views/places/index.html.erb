<div id="carousel-example-generic" class="carousel slide" data-ride="carousel">
  <!-- Indicators -->
  <ol class="carousel-indicators">
    <li data-target="#carousel-example-generic" data-slide-to="0" class="active"></li>
    <li data-target="#carousel-example-generic" data-slide-to="1"></li>
    <li data-target="#carousel-example-generic" data-slide-to="2"></li>
  </ol>

  <!-- Wrapper for slides -->
  	<div class="carousel-inner">
    	<div class="item active">
      <%= image_tag 'salad1.jpg', :class => 'img-responsive' %>
      <div class="carousel-caption">
        Find delicious and healthy dishes
      </div>
    </div>
    <div class="item">
    	<%= image_tag 'salad2.jpg', :class => 'img-responsive' %>
    	<div class="carousel-caption">
      		Looking for healthy but hearty entrees?      		
      	</div>
    </div>
	<div class="item">
    <%= image_tag 'onigiri.jpg', :class => 'img-responsive' %>
    	<div class="carousel-caption">
      		Seek out eclectic Asian treats      
      	</div>
    </div>

  <!-- Controls -->
  <a class="left carousel-control" href="#carousel-example-generic" data-slide="prev">
    <span class="glyphicon glyphicon-chevron-left"></span>
  </a>
  <a class="right carousel-control" href="#carousel-example-generic" data-slide="next">
    <span class="glyphicon glyphicon-chevron-right"></span>
  </a>
</div>

<% @places.each do |place| %>
    <!-- A BUNCH MORE CODE -->
<% end %>

<% @places.each do |place| %>
	<div class="place-box col-xs-10 col-xs-offset-1">
		<h3><%= link_to place.name, place_path(place) %></h3><br />
		<i><%= place.address %></i><br /><br />
		<p><%= place.description %></p><br />
	
		<% if place.user.present? %>
			<small><%= place.user.email %></small>
		<% end %>

		<% if place.comments.present? %>
			<div class="col-xs-12">
    			<blockquote>
      				<% if place.comments.last.humanized_rating.present? %>
        				<b><%= place.comments.last.humanized_rating %></b> -
      				<% end %>
      		
      				<%= place.comments.last.message %></div>
      				<small><%= place.comments.last.user.email %></small>
      			</blockquote>
			</div>
		<% end %>
		</div>
	
<% end %>

<%-#Apple Store pagination style -%>
<div class="col-xs-12 apple_pagination">
	<%= will_paginate @places, :container => false %>
</div>


